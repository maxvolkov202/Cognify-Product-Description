---
name: project_grading-engine-v2
description: Grading Engine V2 — measure-first rebuild of rep scoring (variance bench + A/B arms + reference-anchored/fan-out redesign)
metadata: 
  node_type: memory
  type: project
  originSessionId: 341cffc7-8a01-4ae4-b041-12afa1421778
---

Max wants Cognify's rep grading more accurate per-dimension (esp. tone), faster, and with
less-generic per-dimension feedback. Plan: `C:\Users\MaxVolkov\.claude-personal\plans\yes-i-want-to-prancy-emerson.md`.

**Standing decisions (from the scoping turn):**
- Do NOT rip out deterministic scoring — it's the anti-drift backbone (temp 1.0 swung 30-50pts; pacing is a measurement problem). Make it configurable so an all-LLM arm can be *measured* head-to-head on stability.
- Per-dimension fan-out (6 calls) rejected as the answer — use **grouped fan-out (Content ∥ Delivery+Tone + synthesis)**, ~2x cost, not 6x. Higher-leverage levers: **reference-anchored scoring** (relative>absolute judgment, anchors from the 29-rep bank) and **rubric-decomposition roll-up** (ordinal observations → math, makes tone explainable/low-variance).
- Measure first: Phase 0 bench → prototype arms behind a flag → decide from data → then commit the winner. Max approved "bench first, then redesign+implement once confirmed better."

**Landed (Phase 0+1, uncommitted, tests green):** telemetry `arm` column (migration 0045), `selectScoringArm`+dispatcher in `score.ts` (control renamed to byte-identical `scoreRepControl`), `score-arms.ts` `median-of-n` arm, `scripts/bench/*` (variance-harness = the missing stability instrument, feedback-quality, pricing, run-bench + npm `bench`), `tests/scoring-arms.test.ts`.

**Flags:** `FF_SCORING_VARIANT` (master, default-OFF), `FF_SCORING_VARIANT_PERCENT` (SHA-256 userId bucket, ≥100=anon), `FF_SCORING_VARIANT_ARM`, `FF_SCORING_VARIANT_N`.

**Landed (Phase 2, uncommitted, tests+tsc+lint green):** §0 extraction — all scoring internals moved to new leaf `src/lib/ai/score-shared.ts` (`buildUserPrompt`/`buildSystemBlocks`/`parseAndValidate`/`applyHybridLayer({deliveryMode,thinkingMode})`/`assembleRepScore`/`runSingleCallScore`); `score.ts` = entry points + arm dispatcher + re-exports; `scoreRepControl`=`runSingleCallScore(input)`. **Control proven byte-identical** (moved ~19KB `systemPrompt` literal SHA-256 matched pre-refactor; verbatim slices; done via extraction script to avoid retyping). Arms wired in `score-arms.ts` + `ScoringArm`/`IMPLEMENTED_VARIANT_ARMS`:
- **Arm A** `reference-anchored` (`reference-anchors.ts`): 3 baked-in human-scored anchors (23/73/84) as a 5th CACHED system block after the 4 static ones (cache-prefix preserved); leave-one-out swaps a same-tier substitute when the rep IS an anchor (transcript match).
- **Arm B** `grouped-fanout` (`score-arm-b.ts`): Content ∥ Delivery+Tone (allSettled) + cheap synthesis; SAME cached prefix both calls, scope in user msg; merge→`parseAndValidate`→hybrid→assemble; `FF_ARM_B_DELIVERY_MODE` (deterministic/llm); graceful degrade (call fail→single-call fallback, synth fail→deterministic envelope).
- **Arm C** `tone-decomposed` (`rollup.ts` + Arm B `{toneDecomposition:true}`): tone via ordinal observations (directness/authority/assertiveness) → `rollupTone` (ordinal→points anchored to bands, 0.6-weighted with Hume prosody tone sub-skills when audio) → low-variance explainable tone.

Tests: reference-anchors(27)/score-arm-b(27)/tone-rollup(25). Plus **`all-llm` arm now implemented** (was stubbed): `runSingleCallScore` with `{deliveryMode:"llm",thinkingMode:"llm"}` — control flow, both deterministic layers off; scoring-arms(29) incl. a behavioral test that the config bypasses the pacing override + thinking blend.

**COMMITTED (2026-07-21, on `fix/prompt-selection-bugs` = Max's branch, per his call — NOT a fresh branch):** `14a6d863` Phase 0/1/2 infra + `acfd1cca` all-llm arm. All flag-gated, `FF_SCORING_VARIANT` default OFF → prod scoring unchanged.

**DECISION GATE RUN 2026-07-21 — lean 6-arm sweep. Outcome: keep control, promote nothing (flag stays OFF).** Credits were live (the earlier "providers out of credits" was stale + a grep artifact + a test fixture). Ran on an ISOLATED git-worktree server (:3334, own `.next`) to not disturb Max's :3333 dev window — 192 top-level `/api/score` calls, 0 mock-fallback/429. Lean scope (Max capped ~250 calls): 12-rep stratified accuracy subset + 4-rep variance @N=5 (new `run-bench.mjs --accuracy-ids=<csv>` flag). Report: `plans/bench/DECISION-2026-07-21-lean-sweep.md`.
- **Accuracy composite MAE:** control 1.1 ≈ all-llm 1.3 (tie, within 12-rep noise) << reference-anchored 1.8 < grouped-fanout 2.9 < tone-decomposed 3.0 < grouped-fanout@llm 3.5. No variant beats control.
- **all-llm** = the promising one: matched control composite, BEAT it on delivery MAE (1.6 vs 2.3) + tone (2.5 vs 3.6), variance not worse (22 vs control's own 37). Answers Q1 "drop determinism?" = "not worse on TEXT." GATED on an audio-rep rerun — the deterministic pacing override only earns its keep on audio WPM, which the text subset never tested.
- reference-anchored hurt structure/thinking; grouped-fanout ~17% faster (parallel decode) but regressed clarity 1.4→5.6; tone-decomposed worst on tone (12.3) w/o the Modal prosody worker (needs rollup recalibration).
- Feedback quality flat across arms (groundedness ~0 — gpt-4o doesn't quote spans; pairwise-sim ~0.09 = distinct).

**Next (Max's priorities, 2026-07-21):** DECISION = keep control ("previous grading was better/equal → keep what we had"); no variant promoted, flag stays OFF, nothing to revert (control byte-identical). Grading is **decode-bound**: one gpt-4o call, temp 0.2, `max_tokens:2500` in `score-shared.ts:1516` — ~8.2s p50 is almost all output-token generation. LATENCY LEVERS Max wants pursued:
- **(a) Cut output tokens (biggest, ~linear):** trim the scoring response schema / feedback verbosity. Groundedness is ~0 anyway (model doesn't quote), so there's slack.
- **(b) Clarity-safe parallel decode:** grouped-fanout split the 2500-tok decode across 2 concurrent calls → p50 6.9s vs 8.2s (~17% faster, PROVEN) but regressed clarity 1.4→5.6 because the content/delivery split under-graded clarity. Fix = keep clarity whole, split only the genuinely independent halves.
- **(c) Faster model for cheap parts + streaming:** use a faster/smaller model for synthesis/feedback while gpt-4o does the scoring; stream dimensions to the UI so *perceived* latency drops even if total doesn't.
- Best combo: (a)+(b) stacked. Bench each vs control with `npm run bench`.
Also open: (d) audio rerun control vs all-llm with prosody worker up (`--with-audio`) = the real determinism test, BLOCKED (audio-tone reps have no `audioUrl`; `PROSODY_WORKER_URL` unset/host-tied Modal token). (e) merge to main when Max's bug-fix work on the branch is done (grading is prod-safe, flag OFF; run `/code-review` first).

**LATENCY LEVERS (a) + (a×b) BUILT + BENCHED, 2026-07-21 (uncommitted, tsc+lint+full suite green).** Two new flag-gated arms, both default OFF → prod unchanged, nothing to revert.

**`lean-output` (lever a) = SHIP CANDIDATE.** Control single-call flow, leaner OUTPUT contract only: (1) drops per-dim `signals` narratives (persisted to `dimension_scores.signals` but NEVER rendered — dead output, verified no `.tsx` consumer), (2) halves `feedback` cap 400→160 chars. Impl: `LEAN_SYSTEM_PROMPT` DERIVED from control `systemPrompt` via 3 anchored `requireReplace` subs (throws if base edit breaks an anchor → guardrail holds); `signals` schema → `.optional().default([])` (byte-neutral for control, which still emits it). Wired `runLeanOutput`→`runSingleCallScore(input,{lean:true})`; `lean` flag threaded through runSingleCallScore/buildSystemBlocks/computeScoringPromptBytes. **Benched TWO ways, server-free/RAG-off/in-process** (new `scripts/bench/lean-output-micro.ts` = token/latency mechanism; new `scripts/bench/direct-arm-bench.ts` = MAE-vs-human accuracy — HTTP body carries no token counts + a 2nd dev server risks the node_modules-junction incident, so both call arm fns directly). Micro-bench (6×3): **output −28.2%** (649→466), **latency p50 −16.9%** (7.7s→6.4s; use p50, gpt-4o fat tail dominates means). Accuracy (12 reps × N=3): **composite MAE 2.5 vs control 2.3 = NEUTRAL (inside noise)**. ⚠️ CORRECTION: my first "accuracy-neutral BY CONSTRUCTION" claim was an OVERCLAIM — a single-sample pass scared me (lean 2.7 vs control 1.6) but control's own MAE swung 1.6→2.3 run-to-run = noise; N=3 cleared it. It's neutral by MEASUREMENT not construction (within a dim JSON emits score BEFORE prose so a dim's own score can't move, but earlier dims' prose precedes later scores = weak cross-dim channel). Minor per-dim wobble: worse tone/conciseness ~+1, better thinking/delivery ~-0.3, all in noise. Tests scoring-arms 29→38.

**`lean-split` (lever a×b) = SHELVED, do NOT promote.** grouped-fanout (CONTENT ∥ DELIVERY+TONE + synth) + lean output + a clarity-regression FIX: base CONTENT scope said "spend full token budget on RICH feedback" (4 dims) → flaw-hunting dragged clarity down; lean CONTENT scope removes it + adds anti-compression guard. Wired `runGroupedFanout(input,{lean:true})`; lean scopes `CONTENT_SCOPE_LEAN`/`DELIVERY_SCOPE_LEAN`. Benched 10 reps × N=2 (same-run): **clarity FIX WORKS** (MAE 3.0 ≈ control 3.3 — grouped-fanout's 1.4→5.6 blowup GONE, genuine reusable finding), **latency −42%** (7.8s→4.5s, biggest of any arm), BUT the split still regresses **conciseness 5.1 vs 2.4, tone 5.6 vs 2.6, composite 2.7 vs 2.1** (delivery 4.4 partly a bench artifact — direct inputs pass no word-timings so the deterministic pacing override is bypassed; tone/concise regressions are real). Same accuracy-for-latency tradeoff grouped-fanout was rejected for; keep committed + dormant as a building block (clarity fix is the asset). Tests score-arm-b 27→34.

**COMMITTED `9264cfa1`** on `fix/prompt-selection-bugs` (lean-output + lean-split, flag OFF, /code-review done — fixed 2 findings: leanEdit degrades gracefully instead of throwing at module load so a broken anchor can't 500 control; `signals` default `() => []` avoids array aliasing). tsc+lint+full suite green.

**PIVOT 2026-07-21 (Max) — DO NOT ship lean-output@160, "too lean to get to prod."** New mandate: latency levers that PRESERVE quality — feedback AND scores must stay helpful + accurate (that's the gate). **COST IS NO LONGER A CONSTRAINT** ("I'm okay with the cost increase"). Prod deploy WHEN ready = whole `fix/prompt-selection-bugs` branch is fine (Max OK'd branch scope); Vercel is authed (maxvolkov202) + linked to prod project **cognify-v2**, prod currently has NO FF_SCORING_VARIANT (running control). Explore, bench each vs control on QUALITY (MAE-vs-human + feedback usefulness) AND latency:
1. **Milder feedback trim sweep** — 400→160 was too much. Try 400→{320/280/240}, find the least-aggressive cut that still saves tokens without quality loss. The `lean` flag + `LEAN_SYSTEM_PROMPT` machinery is built (score-shared.ts, derived from control via `leanEdit` anchored subs) — just parameterize the char cap. **Keep the signals-drop regardless** — it's never-rendered dead output = free latency, zero quality cost (the safe floor is "signals-drop + mild/opt-in feedback trim").
2. **Faster model (lever c)** — test a faster model wholesale for "virtually the same output" (cost OK, so even pricier-but-faster counts). Candidates: gpt-4o-mini, a newer fast OpenAI tier, or Claude Haiku 4.5 (`ANTHROPIC_SCORING_MODEL`). Also the split idea: gpt-4o keeps SCORING, faster model does synthesis/feedback. Model id resolves via `MODELS.scoring`/env in `claude.ts`; add a `--model` override to the bench.
3. **Per-skill fan-out (6 parallel calls, one per dimension)** — Max explicitly wants this tested now (cost OK). Each call = tiny single-dim output → all parallel → wall-clock ≈ slowest single-dim + synthesis. Build as new arm `per-skill-fanout`. WATCH: per-dim isolation may hurt calibration like grouped-fanout hurt tone/conciseness — quality is the gate.
**Reusable this-session assets:** `scripts/bench/direct-arm-bench.ts` (SERVER-FREE in-process MAE-vs-human + latency + tokens; add `--model` + new arms), `lean-output-micro.ts`, the `lean` flag machinery, arms dispatcher (score.ts/score-arms.ts). Bench method that works: direct in-process calls, RAG off (`FF_RAG_RETRIEVE=false`), N≥3 samples (single-sample composite MAE is ±10-20 noise — control's own MAE swung 1.6→2.3 run-to-run). **STILL OPEN:** (d) audio determinism rerun (blocked, no audioUrl/PROSODY_WORKER_URL). Report so far: `plans/bench/LATENCY-2026-07-21-lean-output.md`.

**PIVOT EXPERIMENTS BUILT + PARTLY BENCHED (2026-07-21, COMMITTED `7f8fb044` on `fix/prompt-selection-bugs`, all flag-OFF, tsc+lint+scoring-arms(51)+arm-b(34) green).** Report: `plans/bench/LATENCY-2026-07-21-lean-output.md` (PIVOT section). Mandate: latency levers that PRESERVE quality (quality=gate), cost no constraint.
- **Exp 3 `per-skill-fanout` (`runPerSkillFanout` in score-arm-b.ts, 6 parallel single-dim calls + synth): COMPLETE, QUALITY FAIL, dormant only.** Benched 12 reps×N=3: FASTEST arm ever **−43% p50 (7.0→4.0s)** but per-dim isolation wrecks calibration — **tone MAE 12.7 vs 3.0**, thinking 10.5 vs 6.0, composite 4.8 vs 3.0. Same trade grouped/lean-split were rejected for, amplified. Wired into dispatcher + IMPLEMENTED_VARIANT_ARMS. Keep committed dormant; −43% is real IF a future arm closes the calibration loss (give voice calls sibling context, or only fan-out content dims).
- **Exp 1 milder-trim: COMPLETE (12 reps×N=3, gpt-4o). DECISIVE: signals-drop ALONE = the quality-fully-preserved win.** `lean-400` (=`leanFeedbackCap:400`, drop the never-rendered `signals` field, feedback prose BYTE-IDENTICAL to control at ≤400/1-2 sentences): **−15% tokens, −16% p50 (7.7→6.5s), composite MAE 2.2 vs control 2.9 (neutral-to-BETTER), no per-dim regression.** Caps 320/280/240 add ~nothing over 400 (gpt-4o feedback rarely hits even a 240 cap — cap isn't binding); only `lean-160` bites more (−26%) but forces shorter feedback + clarity wobble (the one Max rejected). Machinery: `leanSystemPromptFor(cap)` + `leanFeedbackCap` threaded through runSingleCallScore/buildSystemBlocks; bench arms lean-400/320/280/240/160.
- **Exp 2 faster-model: COMPLETE. DEAD END — gpt-4o is already latency-optimal.** Isolated (control+lean-400, 12 reps×N=3): gpt-4o-mini comp MAE **10.1** (3.5× worse) AND 8.3s (slower); gpt-4.1-mini comp **4.2** AND **14.1s** (~2× slower). Both smaller OpenAI models slower AND worse on the ~25KB-prompt/2500-tok-output shape (reproduces claude.ts benchmark note). Claude Haiku skipped per Max. Bench got `--model`/`--provider` overrides (sets OPENAI_SCORING_MODEL/ANTHROPIC_SCORING_MODEL+AI_PROVIDER before dynamic import).
- **OBJECTIVE STANDPOINT:** the ONLY lever cutting latency with quality FULLY preserved is **signals-drop (~16% faster, literally zero user-visible change — dead field only)**. Everything past 16% costs accuracy: aggressive feedback trim (rejected 160), −43% per-skill fan-out (breaks tone 12.7/thinking), smaller model (slower+worse). To go >16% = a product call, not a measurement one.
- **DECISION (Max, 2026-07-21): SHIP signals-drop.** New first-class arm `signals-drop` = `runSingleCallScore(input,{leanFeedbackCap:400})` COMMITTED `822ef1ef` on `fix/prompt-selection-bugs`. Wired into ScoringArm/IMPLEMENTED_VARIANT_ARMS/dispatcher + bench alias + test. Flag-gated (FF_SCORING_VARIANT_ARM=signals-drop), default OFF, control byte-identical. **/code-review high = 0 correctness findings** (fixed stale doc comment; verified PROD-SAFE: no .tsx renders dimension_scores.signals narratives, sub-skills.ts trend uses subSkillScores from the deterministic mapper = unaffected; write-path change = persisted narratives:[] for bucketed reps, tripwire only for a FUTURE narratives consumer). tsc+lint+scoring-arms(52) green.
- **NEXT (Max's plan, handoff brief written for a NEW chat):** (1) merge `fix/prompt-selection-bugs` → main after review; (2) prod canary — `vercel env add FF_SCORING_VARIANT=true` / `FF_SCORING_VARIANT_ARM=signals-drop` / `FF_SCORING_VARIANT_PERCENT` start SMALL (Max: "start with less", e.g. 10-25) — MIND THE TRAILING-NEWLINE GOTCHA (use --value X --no-sensitive --yes, verify env pull+grep), then `vercel deploy --prod`; watch scoring_telemetry `scoringArm='signals-drop'` latency+MAE, ramp to 100, then optionally fold into control. Prod project **cognify-v2**, currently NO FF_SCORING_VARIANT (control). (3) THEN explore separate-LLM-calls fan-out properly (per-skill −43% is real but breaks tone 12.7/thinking calibration; fix idea = only fan-out CONTENT dims + keep delivery+tone in one holistic call, or give each voice call the sibling dim's context; re-bench vs control). (Earlier quota blocker RESOLVED. Lesson kept: run benches ONE AT A TIME, never concurrent.)

**SHIPPED TO PROD — signals-drop canary LIVE @10% (2026-07-22).** Executed the handoff plan:
- **Merge:** rebased `fix/prompt-selection-bugs` onto `origin/main` (dropped the already-merged prompt-selection commit `0f2871b7`=PR#28's `c6a242f1`, patch-id matched), full-branch `/code-review high` = review-clean (0 correctness findings in reachable path; only finding = the migration-0045 deploy prereq below). tsc+lint+grading suites (scoring-arms 52, ref-anchors 27, arm-b 34, tone-rollup 25, prompt-gen 21, grading-v3 31) green. Opened **PR #29** on Max's fork (`maxvolkov202/Cognify-Product-Description`) + merged → `origin/main` @ `d13952db`. NOTE: `gh` defaults to `upstream` (Bob's `bobsides-AICodebase` repo) — must pass `--repo maxvolkov202/...`. Branches live on `origin` (Max's fork); prod deploys are manual CLI so merge target = Max's fork, not Bob's upstream.
- **PROD DB IS SUPABASE, not Neon** (my old note was wrong). `DATABASE_URL` = `postgres.dunnoccrvrqzsgxsfjuv@aws-1-us-east-2.pooler.supabase.com:6543/postgres` (pgBouncer txn pooler → postgres.js needs `prepare:false`). **The pulled value has a literal trailing `\n`** (the newline gotcha, IN the value) — strip `/\\n$/` before connecting. cognify_v2 is Max's ISOLATED schema (drizzle `schemaFilter`), never touches Bob's `public` v1 tables. **No `__drizzle_migrations` journal on prod → migrations applied via `drizzle-kit push`/manual, NOT journaled** — so surgical raw DDL is safe (no journal to desync).
- **Migration 0045 applied to prod** (the `arm` column was MISSING → canary telemetry would've been blind: insert fails silently under safeDb). Applied the exact idempotent 0045 DDL directly (ADD COLUMN IF NOT EXISTS + CREATE INDEX IF NOT EXISTS) via a throwaway tsx+postgres script. Verified: `arm text NULLABLE` + `scoring_telemetry_arm_idx` present.
- **Env (prod project `cognify-v2`, Vercel, authed maxvolkov202):** `FF_SCORING_VARIANT=true`, `FF_SCORING_VARIANT_ARM=signals-drop`, `FF_SCORING_VARIANT_PERCENT=10` — set via `vercel env add <NAME> production --value X --no-sensitive --yes --force` (NOT stdin → dodges the newline gotcha), verified clean via `vercel env pull` + `cat -A` (no trailing `\n`). **`vercel deploy --prod --yes`** → READY, aliased `cognify-v2-neon.vercel.app`, dpl_AxTkDjcR3mw1Mvynx2khUWL7Ruo3.
- **CONTROL BASELINE (prod, last 48h pre-canary, n=805):** model_p50 **7777ms**, model_p90 9930ms, total_p50 8432ms, out_tok_avg **676**, composite 63.5±17.5, 2 failures, 2 fallbacks. (Control p50 7.78s ≈ bench's 7.7s → bench was representative.) **signals-drop targets: model_p50 ~6.5s (−16%), out_tok ~575 (−15%), composite dist + failure/fallback rates FLAT.** MAE-vs-human NOT measurable on live prod (no ground truth) — that was the bench's job (2.2 vs 2.9, done); prod watch = latency + score-dist + error-rate guardrails only.
- **Watcher running** (persistent Monitor, task b62mrugsw): polls signals-drop telemetry every 15min, notifies on new rows w/ latency+token+failure deltas vs baseline; hourly heartbeat (with 1h control count) while empty so "no traffic" ≠ "arm broken". At ~10% of logged-in users, ~1-2 signals-drop rows/hr → ~a day to accumulate n≥30 for a confident p50.
- **NEXT:** ramp 10→25→50→100 as clean rows confirm no regression (`vercel env add FF_SCORING_VARIANT_PERCENT production --value 25 --force` then redeploy — MUST redeploy for env change to take; DELETE `.tmp-*.ts` before any deploy so they aren't uploaded). Then **step 4 = separate-LLM-call fan-out** (per-skill −43% real but breaks tone 12.7/thinking; fix = fan-out CONTENT dims only + keep delivery+tone in one holistic call, or give voice calls sibling context; bench vs control w/ `scripts/bench/direct-arm-bench.ts`, ONE at a time — concurrent nuked OpenAI quota before). Reusable throwaways this session: `.tmp-watcher.ts` / watch script pattern (postgres.js `prepare:false` + strip `\n` from DATABASE_URL, run from repo root for node_modules resolution).

**STEP 4 DONE — separate-LLM fan-out explored, does NOT beat signals-drop (2026-07-22, COMMITTED `3752d605` on `fix/prompt-selection-bugs`, flag-OFF/dormant, tsc+lint+arm-b(43)+scoring-arms(52) green).** Built `holistic-split` arm = `runGroupedFanout(input,{holistic:true})` — the fan-out CALIBRATION fix. Diagnosis: grouped/per-skill fan-out broke tone because the base `DELIVERY_SCOPE` says "reason ONLY about voice… never the argument's content" → on TEXT reps (no prosody) that STARVES tone of transcript cues control reads it from. Fix (Max's "give voice calls sibling context"): new `CONTENT_SCOPE_HOLISTIC`/`DELIVERY_SCOPE_HOLISTIC` split only the OUTPUT decode, keep FULL rep context + reasoning in both passes (delivery pass licensed to read transcript for tone; content pass keeps anti-nitpick guard, drops grouped's "spend full budget on rich feedback" clarity-killer). Benched vs control+grouped, direct/in-process, gpt-4o, text/no-prosody, RAG off, TWO runs (12×2 then 12×3):
- **Composite NEUTRAL** (run B: 2.5 vs control 2.6). Run A's "beats control 2.1 vs 3.1" was control on the unlucky end of its 1.6–3.1 swing; run B (control 2.6 typical) settles it = no accuracy gain.
- **Latency −18%, output −18%** (consistent) — but only ~2pts better than signals-drop's −16%, at **3× LLM cost** (3 calls).
- **Per-dim tradeoff REAL + consistent both runs: worse conciseness (+1.0/+1.4) + tone (+0.7/+1.1)**, better clarity/structure/delivery. Tone regression FAILS Max's "quality is the gate" bar (signals-drop clears it w/ ZERO regression).
- **Genuine reusable finding:** full-context-split-output ≫ isolated fan-out — fixes the fan-out DISASTER (grouped tone 6.1/clarity 7.1, per-skill tone 12.7 → holistic tone 4.3/clarity 2.4). The calibration loss was the ISOLATION, not the fan-out shape. But keeping quality caps fan-out latency at ~−18% (the −43% only existed BECAUSE per-skill broke calibration) → fan-out has NO latency edge over the far cheaper signals-drop.
- **VERDICT: keep signals-drop shipped; holistic-split committed dormant as best-in-class fan-out building block, NOT a ship candidate.** Report appended to `plans/bench/LATENCY-2026-07-21-lean-output.md` (PIVOT 2026-07-22 section). Bench arm `holistic-split` in direct-arm-bench.ts. Full plan (steps 1-4) now COMPLETE.

**signals-drop RAMPED TO 100% ON PROD + MERGED (2026-07-22).** Max: "if we already approved quality and speed just get it straight to prod" → skipped the staged 10→25→50 ramp, went straight to full. **GUARDRAIL CAUGHT + FIXED before flipping:** at `FF_SCORING_VARIANT_PERCENT>=100` the dispatcher applies the arm even to ANON reps (no userId), and the prod `calibration-drift` cron (`src/app/api/cron/calibration-drift/route.ts`) scores reference reps ANON via `scoreRep` → would've scored the drift baseline with signals-drop = broke the calibration byte-identity guardrail. FIX: added `scoreRepForCalibration` (control-PINNED, bypasses the A/B dispatcher, immune to FF_SCORING_VARIANT) in score.ts; drift cron now uses it. `/api/score` + `/api/score-internal` pass the rep owner's userId → correctly bucketed, unaffected. COMMITTED `12a71a0c`. Then: `FF_SCORING_VARIANT_PERCENT=100` (env add --force, verified clean), `vercel deploy --prod` → READY + health green (ok:true = DB reachable + AI breaker closed) + aliased cognify-v2-neon. **PR #30 MERGED to main (`f1a9c020`)** — carries `3752d605` (holistic-split dormant) + `12a71a0c` (calibration pin). Prod deployment code == merged main. tsc+lint+scoring-arms(53)+arm-b(43) green.
- **NOTE — no live signals-drop telemetry row observed yet:** prod has had ZERO rep-scoring traffic for ~18h+ (last row 2026-07-21 19:24 UTC, pre-canary; low-usage window). Config verified correct end-to-end (env=100, deploy live, dispatcher unit-tested) — the first real rep scored WILL be signals-drop. Watcher was stopped (task b62mrugsw) after ramp. To spot-check later: query `cognify_v2.scoring_telemetry WHERE arm='signals-drop'` (prod = Supabase, DATABASE_URL via `vercel env pull`, strip trailing `\n`, postgres.js `prepare:false`).
- **STATUS: full 4-step plan COMPLETE.** signals-drop live @100% + merged; holistic-split explored → dormant (doesn't beat signals-drop). Optional future cleanup: fold signals-drop into the control path + re-baseline calibration, then retire the FF_SCORING_VARIANT flag (would let the drift cron drop the pin). Not required — current state is stable + correct.

**INCIDENT (fixed):** during cleanup, `git worktree remove --force` followed the node_modules JUNCTION and deleted `.bin`+~45 pkgs from the REAL node_modules before aborting → restored via `npm install` (tests/tsc green, :3333 fine). **Lesson: always `rmdir` the junction link BEFORE `git worktree remove`.** Guardrail still holds: reference-rep path byte-identical, never toggle the variant flag in the CI calibration gate.
