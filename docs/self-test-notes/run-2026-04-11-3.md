# Self-test run — 2026-04-11 #3 (Loop 4)

## What I fixed this loop (no-brainers)

None — all clean.

## Probe results

### Framework adherence scoring — PASS (new dimension tested)

Sent a 75s exec-grade rep with 4 `frameworkNodes` (Opening, Problem,
Solution, Ask). The rep hit all four nodes explicitly.

```
modelVersion:          claude-sonnet-4-6
composite:             91
structuralAdherence:   97
dims:  clarity=91  structure=94  relevance=95  confidence=87  pacing=87  tone=89
```

**structuralAdherence: 97** — the `frameworkNodes` path is working. The
nullable fix from earlier (null → 0 when no framework) confirmed again in
loop 2 where no framework was provided.

Callout quality check — the `clarity/warn` callout flagged:
> "Appendix reference assumes shared context — close the gap. Saying
> 'the data is in the appendix I shared yesterday' works if everyone
> opened it, but if one executive didn't, you've created a trust gap
> at the exact moment you need buy-in."

This is a coaching insight that would cost $200/hr from a real coach. It's
specific, non-obvious, and actionable. The product works.

### Non-business vertical talking points — PASS

Scenario: tell a patient they need knee surgery, reassure on recovery.
Context: 58-year-old, anxious about work.

```
[The News] — soft framing, "surgery is the right path" not "you need surgery"
[What to Expect] — 95% success rate, 6-week recovery, "work timeline depends"
[Address the Fear] — "Missing work is a real concern — validate it"
[Next Steps] — "Invite their questions — don't rush this part"
```

Tone adaptation to healthcare is excellent. Not business-bro, not
clinical. Empathetic and specific to the patient's context.

### Score distribution (complete, across all loops)

| Rep quality | Composite | Notes |
|---|---|---|
| Expert exec with framework (75s) | **91** | structuralAdherence: 97 |
| Strong PREP/BLUF (55s) | **89** | Zero hedges, specific metrics |
| Medium vague (32s, no specifics) | **58** | Appropriate for "just OK" |
| Unicode/emoji | **35** | No crash, scored normally |
| Filler-heavy hedging (18s) | **27** | Correct — rep was bad |
| Gibberish (4s) | **20** | Floor-level, correct |

Distribution is healthy. No score compression at the top or bottom.
Real separation between quality tiers.

### Edge cases — PASS

- **Malformed JSON body** → 400: `"Unterminated string in JSON at
  position 15"` — clean error, no stack trace, no 500.

### Dev log — CLEAN

Zero errors since the Loop #1 fixes. Server stable across 8+ scoring
calls, 3 talking-points calls, and multiple edge-case probes.

## Small-but-judgment (Max to pick)

None new.

## Bigger-thought (needs conversation)

None.

## Loop status

**3 loops complete. No new findings in Loop 4. Product is stable.**
Per playbook rules, stopping the self-test loop.

## Summary across all loops

### Bugs found and fixed (7 total, all no-brainers)

| # | File | Fix | Severity |
|---|---|---|---|
| 1 | `lib/ai/score.ts:42-43` | Callout limits raised (title 60→80, body 200→400) | Medium |
| 2 | `lib/ai/score.ts:50` | `structuralAdherence` made `.nullable()` | Medium |
| 3 | `lib/ai/score.ts:109` | Added length-limit guidance to system prompt | Low |
| 4 | `lib/ai/claude.ts:15` | Framework model Opus→Sonnet (per Max's call) | Config |
| 5 | `lib/session/current-user.ts:56-65` | Guest-user upsert race → `onConflictDoNothing` | High |
| 6 | `lib/ai/score.ts:69-85` | **CRITICAL**: transcript truncation bug in `renderTimedTranscript` | Critical |
| 7 | `app/api/score/route.ts:198-213` | Zod errors polished for clients | Low |

### Judgment items accumulated (2)

- **J1 (fixed)**: Zod error polish — done
- **J4**: Rate limiting is no-op without Upstash. Need either Upstash
  (free tier) or an in-memory fallback before any public-facing deploy.

### Remaining gaps

- Audio transcription end-to-end (Deepgram → word timings → score) can
  only be verified in a browser with a real microphone
- Rate limiting needs Upstash before public deploy
- B1 (harshness calibration) was the transcript truncation bug in disguise
  — scoring distribution is healthy post-fix
