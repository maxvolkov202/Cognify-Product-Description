import { sql } from "./db.mjs";
const real = sql`r.model_version not in ('seed-demo-v1','mock-fallback-v1')`;
console.log(await sql`select (u.email like '%@cognify.test') is_test, count(*)::int n, count(distinct r.user_id)::int users, round(avg(r.composite_score)::numeric,1) comp, round(stddev(r.composite_score)::numeric,1) sd from cognify_v2.reps r join cognify_v2.users u on u.id=r.user_id where ${real} group by 1`);
// human-only v4.1.0 delivery/tone distributions
console.log(await sql`select d.dimension, count(*)::int n, round(avg(d.score)::numeric,1) mean, round(stddev(d.score)::numeric,1) sd, count(distinct d.score)::int uniq, mode() within group (order by d.score) mode, round(100.0*count(*) filter (where d.score = (select mode() within group (order by x.score) from cognify_v2.dimension_scores x join cognify_v2.reps rx on rx.id=x.rep_id join cognify_v2.users ux on ux.id=rx.user_id where x.dimension=d.dimension and rx.rubric_version='v4.1.0' and ux.email not like '%@cognify.test' and rx.model_version not in ('seed-demo-v1','mock-fallback-v1')))/count(*),1) mode_pct from cognify_v2.dimension_scores d join cognify_v2.reps r on r.id=d.rep_id join cognify_v2.users u on u.id=r.user_id where r.rubric_version='v4.1.0' and u.email not like '%@cognify.test' and ${real} group by 1 order by 1`);
// tone by week for human real reps
console.log(await sql`select date_trunc('week',r.created_at)::date wk, count(*)::int n, round(avg(d.score)::numeric,1) tone_mean, round(stddev(d.score)::numeric,1) tone_sd, count(distinct d.score)::int uniq from cognify_v2.dimension_scores d join cognify_v2.reps r on r.id=d.rep_id join cognify_v2.users u on u.id=r.user_id where d.dimension='tone' and u.email not like '%@cognify.test' and ${real} group by 1 order by 1`);
await sql.end();
